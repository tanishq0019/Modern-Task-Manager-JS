# 🚀 Modern Task Manager

A high-performance, responsive To-Do application built to master **Modern JavaScript (ES6+)** concepts.

## 🛠 Key Features
- **Dynamic UI:** Rendered using the `.map()` method for a declarative approach.
- **State Management:** Uses the **Spread Operator** for immutable data updates (React-ready logic).
- **Task Controls:** Ability to add, delete (via `.filter()`), and toggle status.
- **Modern UI:** Glassmorphism design with CSS Flexbox and hover transitions.

## 🧠 Concepts Applied
- **Arrow Functions** & **Destructuring**
- **Template Literals**
- **Array Methods:** `.map()`, `.filter()`
- **Event Listeners:** `click` and `keydown` (Enter key support)
